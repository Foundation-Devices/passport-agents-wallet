# Rust / Cargo
target/
**/*.rs.bk

# Slint codegen (regenerated by build.rs)
ui/gen/

# Editor / OS cruft
.DS_Store
.codex/

# Demo storefront (Node)
demo/storefront/node_modules/
demo/storefront/package-lock.json
# disposable testnet4 merchant key (contains a mnemonic) — regenerate with gen-merchant.mjs
demo/storefront/merchant.json
# in-memory order state (rotating address cursor + orders) — runtime only
demo/storefront/.server-state.json

# host tooling (Python)
**/__pycache__/
*.pyc

# scratch / signed PSBTs
*.psbt
